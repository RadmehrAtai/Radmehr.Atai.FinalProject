{% extends 'base.html.twig' %}

{% block title %}Edit Glasses{% endblock %}

{% block body %}
    <h1>Edit Glasses</h1>

    {{ include('glasses/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_glasses_index') }}">back to list</a>

    {{ include('glasses/_delete_form.html.twig') }}
{% endblock %}
