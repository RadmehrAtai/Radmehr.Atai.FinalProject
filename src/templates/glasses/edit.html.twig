{% extends 'base.html.twig' %}

{% block title %}{{ 'page.glasses.edit'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'app.glasses.edit'|trans }}</h1>

    {{ include('glasses/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-warning" href="{{ path('app_glasses_index') }}">{{ 'app.glasses.back'|trans }}</a>

    {{ include('glasses/_delete_form.html.twig') }}

{% endblock %}
