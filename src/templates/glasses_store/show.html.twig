{% extends 'base.html.twig' %}

{% block title %}GlassesStore{% endblock %}

{% block body %}
    <h1>GlassesStore</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ glasses_store.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ glasses_store.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ glasses_store.address }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ glasses_store.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_glasses_store_index') }}">back to list</a>

    <a href="{{ path('app_glasses_store_edit', {'id': glasses_store.id}) }}">edit</a>

    {{ include('glasses_store/_delete_form.html.twig') }}
{% endblock %}
