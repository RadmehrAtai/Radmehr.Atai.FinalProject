<form method="post" action="{{ path('app_glasses_store_delete', {'id': glasses_store.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this glasses Store?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ glasses_store.id) }}">
    <button class="btn btn-danger">{{ 'app.glassstore.delete'|trans }}</button>
</form>
